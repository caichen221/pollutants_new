<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iscas.samples.distributed.transaction.seata.tcc.server2.mapper.UserDetailsMapper">

    <insert id="insert" parameterType="com.iscas.samples.distributed.transaction.seata.tcc.server2.po.UserDetails" >
        insert into user_details(id, user_id, description)
        values (#{id}, #{userId}, #{description})
    </insert>

    <insert id="delete" parameterType="com.iscas.samples.distributed.transaction.seata.tcc.server2.po.UserDetails" >
        delete from user_details where id = #{id}
    </insert>

</mapper>
