<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iscas.samples.distributed.transaction.seata.tcc.server1.mapper.UserMapper">

    <insert id="insert" parameterType="com.iscas.samples.distributed.transaction.seata.tcc.server1.po.User" >
        insert into user(id, name)
        values (#{id}, #{name})
    </insert>

    <update id="update" parameterType="com.iscas.samples.distributed.transaction.seata.tcc.server1.po.User">
        update user set name = #{name} where id = #{id}
    </update>

    <delete id="delete" parameterType="com.iscas.samples.distributed.transaction.seata.tcc.server1.po.User">
        delete from user where id = #{id}
    </delete>

</mapper>
