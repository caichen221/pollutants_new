<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iscas.samples.distributed.transaction.seata.server1.mapper.UserDetailsMapper">

    <insert id="insert" parameterType="userDetails" useGeneratedKeys="true" keyProperty="id">
        insert into user_details(user_id, description)
        values (#{userId}, #{description})
    </insert>

</mapper>
