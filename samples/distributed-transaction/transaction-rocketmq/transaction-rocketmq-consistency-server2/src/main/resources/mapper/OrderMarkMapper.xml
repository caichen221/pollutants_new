<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iscas.samples.distributed.transaction.rocketmq.server2.mapper.OrderMarkMapper">

    <select id="countByOrderId" resultType="int">
        select count(*) from order_mark where order_id = #{orderId}
    </select>

    <insert id="insertOrderMark">
        insert into order_mark(order_id) values (#{orderId})
    </insert>

</mapper>
