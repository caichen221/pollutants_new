1、CascadeType.PERSIST
    @OneToMany(mappedBy = "app",fetch=FetchType.EAGER,cascade={CascadeType.PERSIST, CascadeType.REFRESH,CascadeType.REMOVE})
    @JsonIgnore
	private List<Organizational> orgs;//当前应用系统拥有的组织结构
	如上，加了CascadeType.PERSIST，表示父实体被持久化的时候，会连同持久化子实体，这个时候如果保存App对象到数据库
	会连同保存App内的orgs
	注意的是不能级联保存，自动生成ID，子表的外键不会自动插入

2、CascadeType.Merge
    只做级联更新

3、CascadeType.REMOVE
    级联删除

4、CascadeType.REFRESH
    级联刷新操作